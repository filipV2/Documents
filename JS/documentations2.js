/**
 * @description return multipication of three elements
 * @param {Number} first component three numbers of multipication
 * @param {Number} second component three numbers of multipication
 * @param {Number} third component three numbers of multipication
 * @return {Number} redult of multiple of three computers
 */
function multipication(first, second, third)
{
    return first * second * third

}
alert(multipication(2,3,4))